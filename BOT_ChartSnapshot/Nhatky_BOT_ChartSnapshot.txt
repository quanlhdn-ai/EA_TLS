Nhật ký 2026-08-06 — BOT_ChartSnapshot

[Combo_Structure_ChartSnapshot_Bot.mq5]
1. Thêm input Inp_EnabledTF (group "Timeframe Filter") — danh sách TF (vd
   "M5,H4") bot này được phép lấy chart/phản hồi lệnh /chart_xx; để trống =
   tất cả 7 TF như cũ. Lý do: khi tích hợp cho 1 bot giao dịch chỉ chạy 2 TF
   (vd H4+M5), bot Chart không cần mở/giữ chart cho các TF không dùng.
2. Thêm BuildEnabledTFList() (parse Inp_EnabledTF, chuẩn hoá + loại trùng),
   IsTFEnabled(), và dựng lại bàn phím Telegram + nội dung /help động
   (BuildKeyboardJson(), SendHelp()) — chỉ hiện nút của các TF đang bật,
   thay cho hằng số KEYBOARD_JSON cố định 7 nút như trước.
3. ProcessCommand(): lệnh /chart_xx không nằm trong Inp_EnabledTF bị từ
   chối kèm cảnh báo, không còn mở chart.
4. Sửa comment mô tả Inp_ChatID: "Group đã được Telegram nâng cấp..." →
   "Group Telegram đã được nâng cấp..." (theo yêu cầu, khôi phục thứ tự
   chữ như bản gốc).
5. Rút gọn mô tả input Inp_EnabledTF — bản đầy đủ quá dài, MT5 Inputs
   dialog không hiển thị hết được.
6. Thêm tính năng Zone Alert (input Inp_EnableZoneAlert, mặc định bật):
   mỗi 3s (trong OnTimer, dùng chung timer với poll Telegram), với từng TF
   trong Inp_EnabledTF, đọc buffer 18/19 (Buy Zone Entry/SL) và 20/21 (Sell
   Zone SL/Entry) của TLS_SMC_Indicator qua handle iCustom riêng
   (GetOrCreateZoneHandle — KHÔNG mở chart, chỉ CopyBuffer, nhẹ hơn nhiều
   so với luồng chụp ảnh vốn phải ChartOpen). Khi giá Bid nằm trong khoảng
   [min(Entry,SL), max(Entry,SL)] lần đầu tiên kể từ khi Zone đó hình
   thành, gửi Telegram "Chạm Zone Buy/Sell <TF>". Trạng thái theo dõi
   (g_zone_buy/sell_entry/stop/alerted[]) song song index với
   g_enabled_tf, tự reset cờ đã báo khi Zone đổi hoặc bị xoá (tránh spam
   mỗi lần timer chạy trong lúc giá còn nằm trong Zone).
7. Giải phóng handle Zone (ReleaseZoneHandles(), IndicatorRelease) trong
   OnDeinit — các chart chụp ảnh vẫn giữ nguyên như quy ước cũ (không đóng).

Nhật ký 2026-08-02 — BOT_ChartSnapshot

[TLS_SMC_Indicator.mq5 — TÊN HIỆN TẠI (đã đổi tên trong ngày hôm nay, xem
 mục "Đổi tên chỉ báo" bên dưới; lúc thực hiện các mục dưới đây file còn
 tên cũ Combo_Structure_MajorSwing_HA_BOS_Zone_Anchor_forBotChart.mq5)]
1. Thêm ShowLastMajorHighLow + LastMajorLineColor — đường mốc "Last Major
   High/Low" mới: luôn bám theo đỉnh/đáy Major Swing gần nhất, độc lập với
   trạng thái Active/Protected (BOS/CHOCH) — không bị "tắt" khi giá phá qua.
2. Thêm DrawLevelGroup()/UpdateLevelLabels() — gom nhãn của 3 nhóm mốc
   (Active/Protected, Weak High/Low, Last Major); khi 2+ mốc trùng giá thì
   gộp chung 1 đường + 1 nhãn "TextA | TextB" thay vì vẽ chồng nhiều object.
3. Sửa CreateTrackingRayWithLabel(): nhãn luôn xoá-và-tạo-lại thay vì cập
   nhật tại chỗ — tránh nhãn bị "kẹt" ở toạ độ cũ. Đồng bộ từ bản indicator
   dùng chung với BOT_TLS.
- So sánh với bản đang chạy thực tế (người dùng copy trực tiếp từ máy để đối
  chiếu): bản đang chạy cũ hơn, thiếu toàn bộ 3 mục trên — đã xác nhận bản
  trong thư mục này là bản mới nhất, cần compile lại để dùng.

[Chuẩn hoá thư mục — chỉ giữ file thật sự cần]
- Đối chiếu bản đang chạy thực tế: Combo_Structure_ChartSnapshot_Bot.mq5 và
  Telegram_ChartBot.mqh giống hệt, giữ nguyên.
- Loại bỏ khỏi thư mục (grep xác nhận không có .mq5 nào ở đây #include hay
  cần tới): CSMC_Engine.mqh, Telegram_Radar.mqh, bản indicator gốc (không
  phải forBotChart), và 3 file .ex5 build cũ (tự sinh lại khi compile).
- Bổ sung 2 file .set lấy từ bản đang chạy (trước đó thư mục chưa có file
  thiết lập nào): config_botgetchart.set (preset của bot),
  chibao_fotbotchart.set (preset hiển thị của indicator — lưu ý file preset
  này lưu từ trước khi có input ShowLastMajorHighLow, cần mở lại MT5 và lưu
  preset mới nếu muốn tuỳ chỉnh đường Last Major).

[Tái cấu trúc thư mục]
- Quy tắc từ nay: TLS_SMC_Indicator.mq5 (tên hiện tại — xem mục "Đổi tên
  chỉ báo" bên dưới) dùng chung giữa BOT_TLS/ và BOT_ChartSnapshot/ — sửa
  ở bot nào cũng phải đồng bộ sang bot còn lại.

[Đổi tên chỉ báo]
1. Combo_Structure_MajorSwing_HA_BOS_Zone_Anchor_forBotChart.mq5 →
   TLS_SMC_Indicator.mq5 (tên cũ quá dài, rút gọn theo quy ước đặt tên đang
   dùng trong repo). Đã cập nhật đồng bộ:
   - Combo_Structure_ChartSnapshot_Bot.mq5: input Inp_IndicatorPath mặc định
     và 2 đoạn StringFind() so khớp tên indicator đã gắn/cần gỡ trên chart.
   - config_botgetchart.set: giá trị Inp_IndicatorPath.
   - Header comment trong chính file indicator (đồng bộ từ BOT_TLS).
   Trước đó, giá trị mặc định trong source (Inp_IndicatorPath =
   "Combo_Structure_MajorSwing_HA_BOS_Zone_Anchor", KHÔNG có hậu tố
   "_forBotChart") thực ra không khớp với bản indicator đang thật sự dùng
   (theo .set là bản forBotChart) — nhân dịp đổi tên, giờ cả source default
   lẫn .set đều thống nhất trỏ đúng 1 tên "TLS_SMC_Indicator", không còn
   lệch nhau nữa.
