Nhật ký 2026-08-14 — BOT_ChartSnapshot

[Combo_Structure_ChartSnapshot_Bot.mq5]
1. SendZoneSignal(): đổi nội dung cảnh báo chạm Zone từ 1 dòng text đơn giản
   ("Chạm Zone Buy/Sell <TF> — SYMBOL\nGiá: ...") sang định dạng tín hiệu đầy
   đủ: "<b>SYMBOL — TF</b>\n🕐 giờ\n👉 Tín hiệu BUY/SELL\n🆘 SL: <giá SL thật
   lấy từ buffer Zone>\n💰 TP: 10-20-30 giá" — dòng TP cố định theo yêu cầu
   người dùng, không tính toán gì thêm.
2. Tách GetCachedChartId() ra khỏi GetOrCreateChartForTF() — chỉ kiểm tra
   chart của 1 TF đã mở sẵn hay chưa, KHÔNG tự mở mới. SendZoneSignal() dùng
   hàm này: nếu chart TF đó đã mở sẵn (do trước đó có ai bấm /chart_xx) thì
   kèm luôn ảnh chụp (Radar.SendPhoto); nếu chưa mở thì CHỦ Ý không tự mở
   chart mới trong luồng cảnh báo Zone (luồng mở chart tốn 3x Sleep(1500) và
   chặn toàn bộ EA vì MQL5 đơn luồng) — lùi về gửi text-only (Radar.
   SendMessage), tránh lag/treo VPS khi nhiều Zone ở nhiều TF chạm gần nhau.
3. Sửa điều kiện "chạm Zone" trong CheckZoneTouches(): trước đó báo khi giá
   nằm bất kỳ đâu trong khoảng [min(Entry,SL), max(Entry,SL)] — tức báo cả
   khi giá đã lọt sâu tới gần mép SL, không đúng ý nghĩa "chạm Entry". Sửa
   lại: chỉ báo khi giá chạm ĐÚNG mép Entry — mép TRÊN (BuyZoneEntryBuffer,
   đỉnh zone) cho Zone Buy, mép DƯỚI (SellZoneEntryBuffer, đáy zone) cho Zone
   Sell — và chưa xuyên thủng mép SL phía đối diện (coi như Zone đã hỏng).
4. Thêm cờ g_zone_buy_armed[]/g_zone_sell_armed[] ("lên đạn"): chỉ báo khi
   giá đã từng ở NGOÀI Zone (phía ngoài mép Entry) kể từ khi Zone hiện tại
   xuất hiện/vừa đổi, sau đó quay lại chạm mép Entry mới báo — tránh bắn
   khống lúc EA vừa khởi động/reload trong khi giá đang nằm sẵn giữa Zone;
   đồng thời cho phép báo lại nếu giá thoát hẳn ra ngoài rồi chạm lại lần
   nữa (tái vũ trang, không chỉ báo đúng 1 lần duy nhất cho cả vòng đời Zone).
5. Thêm VNNow() (= TimeGMT() + 7 giờ) thay cho TimeCurrent() khi hiển thị
   timestamp — TimeCurrent() trả về giờ SERVER của sàn (không phải giờ máy
   VPS, và không chắc là giờ VN vì tuỳ broker đặt múi giờ khác nhau).
   TimeGMT() tự quy đổi giờ server -> GMT chuẩn dựa theo offset broker công
   bố nên cộng thêm 7 tiếng ra đúng giờ VN mà không cần biết trước broker
   lệch bao nhiêu. Áp dụng cho cả caption ảnh /chart_xx lẫn tin nhắn tín
   hiệu Zone, có ghi chú "(giờ VN)" cho rõ.
6. Thêm AdoptExistingCharts(), chạy trong OnInit: quét lại các chart đang mở
   sẵn từ phiên trước (cache g_cache_tf/g_cache_chart_id trong RAM bị mất
   mỗi khi EA reload dù chart vẫn còn mở trên terminal) để nhận lại vào
   cache — không ChartOpen, không Sleep nên gần như miễn phí. Nhờ đó cảnh
   báo Zone có thể kèm ảnh ngay từ đầu thay vì phải đợi người dùng bấm lại
   /chart_xx sau mỗi lần EA reload, đồng thời tránh mở chart trùng lặp.
7. Sửa AdoptExistingCharts(): loại trừ chính chart đang chạy EA này (so
   sánh id == ChartID()). Trước đó nếu chart host trùng TF với 1 mục trong
   Inp_EnabledTF (thường là M1, vì EA hay được gắn trên chart M1) thì bị
   nhận nhầm làm "chart mồi" cho TF đó — khiến /chart_M1 và cảnh báo Zone M1
   dùng luôn chart sống đang chạy EA (có thể thiết lập hiển thị/template
   khác chuẩn chụp ảnh) thay vì mở 1 tab mồi riêng như các TF khác. Từ giờ
   M1 cũng luôn được ChartOpen riêng, đi qua đúng luồng thiết lập chuẩn
   giống mọi TF khác.
8. Thêm tính năng Zone Formed (Limit) Alert: input Inp_EnableZoneFormedAlert
   (bật/tắt) + Inp_ZoneFormedTF (danh sách TF, định dạng giống Inp_EnabledTF
   nhưng ĐỘC LẬP hoàn toàn — có thể khác danh sách TF lấy chart).
   CheckZoneFormed()/CheckOneZoneFormed() phát hiện Entry/SL của Zone (Buy,
   Sell riêng biệt) đổi khác lần đọc trước để báo "Zone mới hình thành - đặt
   Limit tại...". Lần đọc đầu tiên sau khi EA khởi động/reload chỉ ghi nhận
   Zone hiện có làm mốc, KHÔNG báo (không rõ Zone đó đã hình thành từ bao
   lâu trước khi EA chạy).
9. Thêm GetPipSize() — sao y quy ước từ BOT_TLS/TLS_SMC_CSV_Bot.mq5 (vàng/
   gold = 0.1, JPY = 0.01, 5/4-digit = 0.0001, 3/2-digit = 0.01, còn lại
   Point*10) để đồng bộ đơn vị pip toàn repo. Thêm ComputeSLPadPips(): chia
   độ rộng Zone (pips) thành 5 phần bằng nhau, lấy 1 phần so với thang
   30-35-40-45-50 pips, chọn mốc đầu tiên ≥ giá trị đó làm khoảng đệm (Zone
   càng rộng đệm càng nhiều, tối đa 50 pips) — theo đúng công thức người
   dùng yêu cầu, đã đối chiếu khớp ví dụ họ đưa ra (biên 4000 + 40 pips ×
   $0.1 = 4004). Thêm ComputeAdjustedSL(): đẩy SL ra xa mép Zone theo đệm
   trên rồi MathRound() về số nguyên (vd 4023.15 -> 4023). Áp dụng CHUNG cho
   cả SendZoneSignal (chạm Zone) và SendZoneFormedSignal (Zone mới hình
   thành) — chỉ đổi giá trị HIỂN THỊ, logic phát hiện chạm/xuyên Zone vẫn
   dùng SL gốc chưa đệm từ buffer indicator, không đổi.
10. Tách SendSignalMessage() dùng chung cho cả 2 loại tín hiệu (chạm Zone /
    Zone mới hình thành) — cùng 1 logic kèm ảnh nếu chart TF đó đã cache sẵn,
    lùi về text-only nếu chưa, tránh lặp code giữa SendZoneSignal và
    SendZoneFormedSignal.
11. Tách ParseTFList() dùng chung cho Inp_EnabledTF và Inp_ZoneFormedTF —
    trước đó BuildEnabledTFList() tự parse riêng, giờ dùng lại đúng 1 hàm
    cho cả 2 input.
12. Bỏ hẳn nút/lệnh help theo yêu cầu người dùng (đã ghim sẵn tin nhắn hướng
    dẫn, không cần bot gửi lại): bước đầu bỏ hàng nút "/help" khỏi bàn phím
    (BuildKeyboardJson) nhưng vẫn giữ lệnh gõ tay, đổi tên từ "/start"/"/help"
    sang "/chart_help" — tránh trùng tên lệnh chung chung với bot giao dịch
    khác trong CÙNG group Telegram (Telegram mặc định chuyển MỌI tin nhắn
    bắt đầu bằng "/" tới TẤT CẢ bot trong group, kể cả lệnh không nhắm tới
    bot đó — đây là nguyên nhân người dùng thấy bot "chạy theo" lệnh của bot
    giao dịch). Sau đó theo yêu cầu tiếp theo, bỏ HẲN luôn "/chart_help" và
    xoá SendHelp() (không còn ai gọi). ProcessCommand() giờ CHỈ phản hồi
    đúng 7 lệnh /chart_M1..D1, mọi lệnh khác (kể cả của bot khác trong
    group) bị bỏ qua hoàn toàn. BuildKeyboardJson() trả về
    {"remove_keyboard":true} khi n==0 (chưa bật TF nào) thay vì hiện nút
    help như trước.

Nhật ký 2026-08-06 — BOT_ChartSnapshot

[Combo_Structure_ChartSnapshot_Bot.mq5]
1. Thêm input Inp_EnabledTF (group "Timeframe Filter") — danh sách TF (vd
   "M5,H4") bot này được phép lấy chart/phản hồi lệnh /chart_xx; để trống =
   tất cả 7 TF như cũ. Lý do: khi tích hợp cho 1 bot giao dịch chỉ chạy 2 TF
   (vd H4+M5), bot Chart không cần mở/giữ chart cho các TF không dùng.
2. Thêm BuildEnabledTFList() (parse Inp_EnabledTF, chuẩn hoá + loại trùng),
   IsTFEnabled(), và dựng lại bàn phím Telegram + nội dung /help động
   (BuildKeyboardJson(), SendHelp()) — chỉ hiện nút của các TF đang bật,
   thay cho hằng số KEYBOARD_JSON cố định 7 nút như trước.
3. ProcessCommand(): lệnh /chart_xx không nằm trong Inp_EnabledTF bị từ
   chối kèm cảnh báo, không còn mở chart.
4. Sửa comment mô tả Inp_ChatID: "Group đã được Telegram nâng cấp..." →
   "Group Telegram đã được nâng cấp..." (theo yêu cầu, khôi phục thứ tự
   chữ như bản gốc).
5. Rút gọn mô tả input Inp_EnabledTF — bản đầy đủ quá dài, MT5 Inputs
   dialog không hiển thị hết được.
6. Thêm tính năng Zone Alert (input Inp_EnableZoneAlert, mặc định bật):
   mỗi 3s (trong OnTimer, dùng chung timer với poll Telegram), với từng TF
   trong Inp_EnabledTF, đọc buffer 18/19 (Buy Zone Entry/SL) và 20/21 (Sell
   Zone SL/Entry) của TLS_SMC_Indicator qua handle iCustom riêng
   (GetOrCreateZoneHandle — KHÔNG mở chart, chỉ CopyBuffer, nhẹ hơn nhiều
   so với luồng chụp ảnh vốn phải ChartOpen). Khi giá Bid nằm trong khoảng
   [min(Entry,SL), max(Entry,SL)] lần đầu tiên kể từ khi Zone đó hình
   thành, gửi Telegram "Chạm Zone Buy/Sell <TF>". Trạng thái theo dõi
   (g_zone_buy/sell_entry/stop/alerted[]) song song index với
   g_enabled_tf, tự reset cờ đã báo khi Zone đổi hoặc bị xoá (tránh spam
   mỗi lần timer chạy trong lúc giá còn nằm trong Zone).
7. Giải phóng handle Zone (ReleaseZoneHandles(), IndicatorRelease) trong
   OnDeinit — các chart chụp ảnh vẫn giữ nguyên như quy ước cũ (không đóng).

Nhật ký 2026-08-02 — BOT_ChartSnapshot

[TLS_SMC_Indicator.mq5 — TÊN HIỆN TẠI (đã đổi tên trong ngày hôm nay, xem
 mục "Đổi tên chỉ báo" bên dưới; lúc thực hiện các mục dưới đây file còn
 tên cũ Combo_Structure_MajorSwing_HA_BOS_Zone_Anchor_forBotChart.mq5)]
1. Thêm ShowLastMajorHighLow + LastMajorLineColor — đường mốc "Last Major
   High/Low" mới: luôn bám theo đỉnh/đáy Major Swing gần nhất, độc lập với
   trạng thái Active/Protected (BOS/CHOCH) — không bị "tắt" khi giá phá qua.
2. Thêm DrawLevelGroup()/UpdateLevelLabels() — gom nhãn của 3 nhóm mốc
   (Active/Protected, Weak High/Low, Last Major); khi 2+ mốc trùng giá thì
   gộp chung 1 đường + 1 nhãn "TextA | TextB" thay vì vẽ chồng nhiều object.
3. Sửa CreateTrackingRayWithLabel(): nhãn luôn xoá-và-tạo-lại thay vì cập
   nhật tại chỗ — tránh nhãn bị "kẹt" ở toạ độ cũ. Đồng bộ từ bản indicator
   dùng chung với BOT_TLS.
- So sánh với bản đang chạy thực tế (người dùng copy trực tiếp từ máy để đối
  chiếu): bản đang chạy cũ hơn, thiếu toàn bộ 3 mục trên — đã xác nhận bản
  trong thư mục này là bản mới nhất, cần compile lại để dùng.

[Chuẩn hoá thư mục — chỉ giữ file thật sự cần]
- Đối chiếu bản đang chạy thực tế: Combo_Structure_ChartSnapshot_Bot.mq5 và
  Telegram_ChartBot.mqh giống hệt, giữ nguyên.
- Loại bỏ khỏi thư mục (grep xác nhận không có .mq5 nào ở đây #include hay
  cần tới): CSMC_Engine.mqh, Telegram_Radar.mqh, bản indicator gốc (không
  phải forBotChart), và 3 file .ex5 build cũ (tự sinh lại khi compile).
- Bổ sung 2 file .set lấy từ bản đang chạy (trước đó thư mục chưa có file
  thiết lập nào): config_botgetchart.set (preset của bot),
  chibao_fotbotchart.set (preset hiển thị của indicator — lưu ý file preset
  này lưu từ trước khi có input ShowLastMajorHighLow, cần mở lại MT5 và lưu
  preset mới nếu muốn tuỳ chỉnh đường Last Major).

[Tái cấu trúc thư mục]
- Quy tắc từ nay: TLS_SMC_Indicator.mq5 (tên hiện tại — xem mục "Đổi tên
  chỉ báo" bên dưới) dùng chung giữa BOT_TLS/ và BOT_ChartSnapshot/ — sửa
  ở bot nào cũng phải đồng bộ sang bot còn lại.

[Đổi tên chỉ báo]
1. Combo_Structure_MajorSwing_HA_BOS_Zone_Anchor_forBotChart.mq5 →
   TLS_SMC_Indicator.mq5 (tên cũ quá dài, rút gọn theo quy ước đặt tên đang
   dùng trong repo). Đã cập nhật đồng bộ:
   - Combo_Structure_ChartSnapshot_Bot.mq5: input Inp_IndicatorPath mặc định
     và 2 đoạn StringFind() so khớp tên indicator đã gắn/cần gỡ trên chart.
   - config_botgetchart.set: giá trị Inp_IndicatorPath.
   - Header comment trong chính file indicator (đồng bộ từ BOT_TLS).
   Trước đó, giá trị mặc định trong source (Inp_IndicatorPath =
   "Combo_Structure_MajorSwing_HA_BOS_Zone_Anchor", KHÔNG có hậu tố
   "_forBotChart") thực ra không khớp với bản indicator đang thật sự dùng
   (theo .set là bản forBotChart) — nhân dịp đổi tên, giờ cả source default
   lẫn .set đều thống nhất trỏ đúng 1 tên "TLS_SMC_Indicator", không còn
   lệch nhau nữa.
